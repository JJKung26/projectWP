<%- include('partials/header') %>

<div class="container" style="max-width: 500px; text-align: center; margin-top: 60px;">
    <div class="card" style="padding: 40px 20px;">
        <div style="font-size: 3rem; margin-bottom: 10px;">⚠️</div>
        <h2 style="color: var(--brand-red); margin-bottom: 12px;">เกิดข้อผิดพลาด</h2>
        <p style="color: var(--text-secondary); margin-bottom: 24px;"><%= message %></p>
        <a href="javascript:history.back()" class="btn btn-secondary">ย้อนกลับ</a>
    </div>
</div>

<%- include('partials/footer') %>
